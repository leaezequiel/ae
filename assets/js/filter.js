//let events = datos.events;
/* 
function captureData() {
    let texto = document.getElementById('id_search').value
    let checks = Array.from(document.querySelectorAll('.class_checks:checked')).map(each => each.value)
    let filtro = datos.events.filter(each => {
        return (each._id.includes(texto)) && (checks.length === 0 || checks.includes(each.category))
    })
    console.log(filtro)
    if (filtro.length>0) {
        createTemplate(filtro)
    } else {
        notFound('#cardContainer')
    }
} */